# NEXUS EXCHANGE ERP — development and verification dependencies (PART 48, PART 56)
#
# Used by CI before running lint, type checks and tests. Runtime pins stay in
# requirements.txt; installing this file does not upgrade them.

-r requirements.txt

pytest==9.1.1
pytest-asyncio==1.4.0
pytest-cov==7.1.0
httpx==0.28.1
mypy==2.3.1
ruff==0.16.7
PyYAML==6.0.3
